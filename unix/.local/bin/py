#!/bin/sh
# Uses Ipython 3 by default

if test "$#" -eq 0; then
  xonsh || ipython3 --profile=terminal || python3
elif test "$#" -eq 1 -a ! -a "$1"; then
  ipython3 --profile="$1"
else
  python3 "$@"
fi

