# Use English
LANG=C
# Do not default to EDITOR, but VISUAL
EDITOR=${VISUAL-${EDITOR-vi}}
# Use the default browser
R_BROWSER=${BROWSER}
# Load personal profile
R_PROFILE_USER=${HOME}/.config/r/profile.r
# TODO: Make sure that R does not polute the top level directory
#R_LIBS=${HOME}/.local/lib/r
#R_LIBS_USER=${HOME}/.local/lib/r
#R_DEFAULT_PACKAGES=tidyverse
# R_HISTFILE=${HOME}/.local/share/r/history

